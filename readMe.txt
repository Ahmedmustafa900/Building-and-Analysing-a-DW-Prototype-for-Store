1. First execute the createDW.sql file to create the tables in the database(this will create tables in the star schema)
2. execute the INLJ file to poppulate the tables.
3. execute the queriesDW file to see the results of the OLAP queries.(as all queries are in the same file to see all 
   outputs the results will have to be scroled.
